# After 47 — 일상으로

화재 피해자에게 **지금 무엇을 먼저 해야 하는지, 무엇을 하면 안 되는지,
다음에 누구에게 넘어가야 하는지**를 개인 상황에 맞춰 알려주는 웹서비스.

브랜드는 2층이다 — 서비스명 **`일상으로`**, 설명 **`화재피해 회복
내비게이션`**(D-023). 화면 좌상단과 랜딩 대제목이 서비스명이고, 설명은
랜딩 소라벨과 문서 제목에 쓴다.

사용자는 집이 불탄 직후의 사람이다. 기본 진입점은 **화재 당일, 현장 정리가
끝나고 조사관이 철수하면서 명함에 붙은 QR을 건네는 시점**이다 — 불이 꺼지고
몇 분에서 몇 시간 뒤다.

**사용자는 자기 집에 화재 피해를 입은 당사자이며, 조사 결과에 따라
가해자가 될 수도 있는 사람이다.** V1의 스코프가 이 한 문장이다 —
"위층 화재로 우리 집이 젖은 남의집-화재 피해자"는 여기 없다. 물 피해 두
축(`water_damage_home`·`water_damage_neighbor`)과 그 안내들이 전부 이
전제 위에 서 있다.

**며칠 뒤가 아니고, 화재조사서가 나온 뒤는 더더욱 아니다.** 조사서 수령은
7일이 지나서야 `q-report`가 따로 묻는 별개 사건이고, 그것이 `after_report`
섹션을 여는 열쇠다. 진입 시점을 조사 완료 이후로 읽으면 첫 화면의 대상이
"조사가 끝난 사람"이 되어 `today`·`standing`이 통째로 의미를 잃는다.

**정신이 없고, 길게 읽을 수 없고, 잘못된 안내를 그대로 따를 수 있는 사람**
이라는 전제로 모든 것을 판단한다.

## 문서

작업 전에 `doc/` 를 읽어라.

| 파일 | 내용 |
|---|---|
| `doc/problem-definition.md` | 왜 만드는가. 검증된 근거와 미검증 가설 |
| `doc/design-decisions.md` | 왜 이렇게 만드는가. D-001~D-023 |
| `doc/SCHEMA.md` | 데이터 구조 정의 |
| `doc/fact-layer-data.md` | 콘텐츠 원본과 출처 |
| `doc/reference-case.md` | 실제 사례. **스키마 검증 기준** |
| `doc/ui-research.md` | UI 패턴·재난 UX 리서치. **참고자료 — 확정 아님** |

**설계를 바꾸려면 먼저 `design-decisions.md`에서 해당 결정을 찾아라.**
결정을 뒤집을 때는 그 문서에 폐기 이력과 이유를 남긴다.

## 스택

정적 사이트 + 브라우저 규칙엔진. **서버 없음.** (D-002)

- 순수 JS(ESM). 빌드 도구 없이 시작
- 상태는 URL 토큰 + localStorage
- 데이터는 `data/*.json` 정적 파일

Django는 검토했다가 폐기했다. 서버 저장을 하지 않기로 한 순간 할 일이
없어졌다. 되살리지 마라.

## 절대 깨면 안 되는 것

### 1. 저장 계층과 데이터 로딩은 함수 뒤에 둔다 (D-002)

```js
loadState(token) / saveState(token, state)   // localStorage → 나중에 DB
loadData()                                    // JSON 파일 → 나중에 API
```

정식 채택되면 서버로 확장한다. 이 함수 안쪽만 바꿔서 되도록 유지한다.
**localStorage 호출이 코드 여기저기 흩어지면 이 결정이 무너진다.**

세 함수는 전부 `async`다. localStorage는 동기지만 서버는 아니다.
동기로 만들면 서버로 옮길 때 호출부가 전부 바뀐다. **동기로 되돌리지 마라.**

`src/storage.js` 밖에서 `localStorage`를 부르면 `test/storage.test.mjs`가
실패한다. 백엔드를 갈아끼울 수 있다는 것은 `test/storage.contract.mjs`를
메모리·localStorage·비동기 서버 흉내 셋에 돌려서 증명한다.
**새 백엔드를 만들면 이 계약을 통과시키는 것이 조건이다.**

### 2. 순서를 데이터에 박지 않는다 (D-001)

Action에 순서 번호나 전역 정렬 배열을 만들지 마라. 순서는 `depends_on`
쌍 관계와 `when`으로 런타임에 계산된다. 조건이 바뀌면 재계산되어야 한다.

### 3. 조건은 단순 매칭만 (D-010)

`applies_when`은 키-값 매칭. 같은 키 안의 배열은 OR, 다른 키끼리는 AND.
논리 표현식 파서를 만들지 마라. 조건이 복잡하면 Action을 쪼개라.

### 4. Action은 사라지지 않고 상태를 갖는다 (D-011)

`해당` / `조건부` / `제외` / `미판정`. 조건에 안 맞는다고 화면에서
지우지 마라. "이런 지원이 있지만 당신은 이 사유로 해당되지 않는다"도 정보다.

네 번째 상태 `미판정`은 D-019 §6이 추가했다. **판정 결과가 아니라 판정
이전이다** — 자치구를 아직 안 골랐거나, 그 구가 실제로 보는 보험 키가
`unknown`인 경우다. "모른다"를 "아니다"로 처리하면 지원 대상인 사람이
제외로 보인다.

### 5. `standing`은 금지사항 전용

"하지 마세요"만 담는다. "무료로 신청할 수 있습니다"는 `anytime`이다.
이 경계가 흐려지면 "당분간 하지 마실 것" 블록에 신청 안내가 섞인다.

### 6. 물 피해는 독립된 두 축이다 — 합치지 마라

`water_damage_home`(우리 집이 젖었나)과 `water_damage_neighbor`(이웃
세대가 젖었나)는 각각 `true|false|"unknown"`이다. 물 피해를 **받은 쪽과
준 쪽은 지시가 정반대**라("관리사무소에 알리세요" vs "책임을 인정하지
마세요") 한 값으로 묶으면 반대 지시가 함께 뜬다.

**옛 `water_damage_role`(victim·causer·both·none·unknown) 5지선다를
되살리지 마라.** 사용자가 `both`를 직접 고르는 UX였고, "우리 집은 젖었는데
아래층은 모르는" 상태를 표현할 수 없었다(D-016 해소).
`test/engine.test.mjs`가 네 조합을 검증한다.

### 7. 책임 경계 (D-006)

**말한다** — 이 상태는 이런 성질이고(사실), 그래서 이걸 해야 하고(행동),
이건 하면 안 되고(금지), 이 순서로 해야 한다(선후).

**말하지 않는다** — 구체적 시공 방법, 약품·자재 지정, 업체 추천,
보험금 액수, 법적 책임 비율, 수급 자격 확정.

"분말은 부식성이 있으니 청소하세요"는 말한다.
"이 세제로 이렇게 닦으세요"는 업체 영역이다.

### 8. 광고·업체 추천 없음 (D-007)

공공기관 QR로 진입한 사용자는 업체 광고를 "소방서가 추천한다"로 받아들인다.
수익모델을 넣지 마라.

## 콘텐츠 작성 규칙

- `title` 한 줄, `summary` 한 문장, `body`는 펼쳐야 나온다
- 정신없는 사람이 읽는다. 법률 용어는 괄호로 풀어준다
- 소요기간·비용·기한은 숫자로. "빠른 시일 내"가 아니라 "3영업일 이내"
- 출처가 업체 블로그면 `source_grade: "참고-업체"`. **법정형과 정량 수치의
  근거로 쓰지 마라**
- 확인 못 한 것은 추정하지 말고 `source_grade: "출처필요"`

## 데이터 현황

- Action **58개**(피해자 57 + 조사관 1), 자치구 **25개**, 질문 19개 —
  **실제로 묻는 것은 15~18개**다. 그중 화재 발생일과 지역은 설문이 아니라
  확정 화면 `기본 확인`에서 받는다. 자치구·소유형태에 따라 줄고(`ask_when`),
  화재 7일이 지나면 조사서 수령 질문이 하나 늘어난다
  (7일 전 15~17 / 7일 후 16~18. `q-report`가 `elapsed_bucket`을 본다).
  최대에 닿는 것은 보험금 수령을 묻는 두 구(양천·관악)다
- **자치구는 서울 25개 전수다** — 조례 보유 13 / 미보유 12. 조례 13개는
  `raw_13/`의 원문에서 직접 판독했다. 판정 변종은 보험 4 · 거주 3 · 계열 3
- 금액은 **25개 구 전부 미상**이다. 위임 방식은 별표 2(양천·영등포) ·
  규칙 2(관악·성동) · "구청장이 따로 정한다" 5 · 위임 조항 없음 4인데
  하위 규범은 전수조사에서 0건이고 별표 본문도 미확보다.
  **금액이 비어도 화면이 깨지지 않아야 한다** (D-003). `dept`도 신규 9개
  구는 `null`이라 degrade가 예외가 아니라 기본 경로다

## 검증

```
npm test                       일곱 다 실행

node test/engine.test.mjs      판정 결과 출력 + 수손 가해/피해 분기
node test/engine.snapshot.mjs  계기판 — 125조합이 기준선과 같은가
node test/questions.test.mjs   설문만으로 레퍼런스 케이스에 도달하는가
node test/districts.test.mjs   25개 구가 변종별로 제대로 판정되는가
node test/view.test.mjs        화면이 무엇을 그릴지 브라우저 없이 검사한다
node test/walk.test.mjs        화면을 실제로 밟는다 — 여정 넷 (DOM 스텁)
node test/storage.test.mjs     백엔드를 갈아끼워도 같은 계약을 지키는가

npm run serve                  정적 서버(http://localhost:5173)
```

레퍼런스 케이스(오피스텔 임차인 / 현관등 발화 / 제조물 의심)가 끝까지
도는지 확인한다. `doc/reference-case.md` §5의 체크리스트가 기준이다.

`questions.test.mjs`는 **손으로 채운 state가 아니라 설문을 실제로 돌려서**
확인한다. 엔진 테스트는 통과하는데 UI에서는 안 도는 상황을 잡는 것이
목적이다. `ask_when`이 죽은 조건이 되는 것, 엔진이 읽는 state 키에 질문이
없는 것을 여기서 막는다.

`districts.test.mjs`는 자치구 데이터를 **데이터 검사와 판정 검사로 나눠**
본다. 앞쪽은 `districts.json`만 보고(25개 전수·enum 어휘·변종이 0이 되지
않는지), 뒤쪽은 변종별 state를 설계해 `evaluate()`를 실제로 돌린다.
**핵심 한 줄은 "각 구의 조례 행 수 == `support_items` ∩ 네 항목"**이다 —
`skip`된 항목은 어느 버킷에도 행이 없으므로, 이 등식 하나가 매핑 오류를
대부분 잡는다. `meal`에 대응 Action이 0건인 것은 **의도**이고 그 사실이
테스트로 박혀 있다(D-020).

`view.test.mjs`는 **화면의 판단을 브라우저 밖에서** 본다. `src/ui/`의
뷰모델은 전부 DOM을 모르는 순수함수이고(`view.js`·`entry.js`·`result.js`·
`rows.js`·`format.js`·`contacts.js`), DOM 코드는 그것을 받아 그리기만 한다.
**판단이 브라우저 안에 숨으면 계기판이 못 본다** — 엔진에서 했던 것과 같은
이유다. 확정 카피는 **문자열 그대로** 비교하므로 조사 하나가 바뀌면 걸린다.
특히 지키는 것 — 설문 뷰모델은 **분모를 반환하지 않는다**(질문 수가 답에
따라 변해 "3/18"이 거짓이 된다).

`walk.test.mjs`는 **연결**을 본다. 뷰모델이 아무리 옳아도 `app.js`가 그것을
안 부르면 사용자에게는 아무 일도 일어나지 않는다. DOM 스텁(`test/dom.stub.mjs`)
위에서 여정 넷을 끝까지 밟는다 — 첫 방문 전체 · 재방문 게이트 ·
미판정에서 질문으로 갔다 돌아오기 · 안내 범위의 두 갈래.
**진짜 브라우저가 아니다** — 여백·색·줄바꿈·overflow는 여기서 못 보고
배포 뒤 실기기 확인의 몫이다.

`storage.test.mjs`는 D-002가 지켜지는지를 두 방향에서 본다 — 계약 테스트가
`storage.js`의 동작을 보고, 누수 탐지가 다른 파일이 몰래 저장소를 만지는지를
본다. 둘 다 있어야 "안쪽만 바꾸면 된다"가 증명된다.

`engine.snapshot.mjs`가 계기판이다. 페르소나 25 × 시각 5 = **125조합**을
`fixtures/engine-baseline.json`과 통째로 비교한다. **기준선은 "옳은 출력"이
아니라 "현재 출력"이다** — 무엇이 움직였는지를 한 글자도 빠짐없이 보여주는
것이 이 파일의 일이다. 의도한 변화라면 `--update`로 다시 뜬다.
**스냅샷을 갱신하기 전에 diff를 유형별로 세어라** — 항목 추가/삭제/순서
변화/섹션 변화/count 변화. "예고한 종류만 나왔는가"가 완료 기준이다.

비교와 별개로 **불변식 일곱**이 돌고 이쪽은 `--update`로도 안 넘어간다.
`excluded` 버킷이 비어 있지 않을 것 · `조건부`/`제외`/`미판정`을 밟는 조합이
각각 있을 것 · `waiting` 버킷이 비어 있지 않을 것 · `blocked`에 불가역이
0건일 것 · 잠김(`locked`)이 최소 1건 있을 것. 비교만으로는 계기판이 눈을
잃어도(페르소나가 사라지거나 판정 경로가 끊겨도) 기준선이 같이 바뀌어
통과하기 때문에 따로 둔 검사다.

**UI를 만들 때도 이 케이스로 확인하라.** 엔진은 `rank`만 주고 자르지
않는다 — **자를지 말지는 UI가 정한다**(D-019 §2). 지금 확정된 IA에는
자르는 자리가 없다: 체크리스트는 rank 순으로 전량을 싣고, 접는 것은
타임라인 노드와 주제 상세의 조건부·제외 블록이다. D-021의 카드 예산
1+4는 그 화면과 함께 폐기됐다(D-023). `standing`(금지)과 `missed`(지나간
것)는 애초에 순위 밖이고 `먼저 볼 내용`이 맡는다.

## 화면 (D-023)

```
첫 방문:  랜딩 → 기본 확인 → 질문(MASTER) → 전환 → 내 회복 경로 HOME
재방문:   경과시간 게이트 → (남은 질문이 있으면 설문 →) HOME

HOME ⇄ { 먼저 볼 내용 · 체크리스트 · 알아둘 내용 ·
         회복 타임라인 · 주제별로 보기 } ⇄ 상세(주제 · Action · 아직 확인 못 함)
```

확정 화면 패킷이 기준이다(`ilsanguro_frontend_FINAL_2026-08-30`, 저장소 밖).
**회색 와이어프레임은 구조·위계·카피의 기준이지 최종 색이 아니다.**

- `index.html`은 **리포 루트**다. `loadData`의 base가 `"data"`인 상대경로라
  절대경로로 바꾸면 GitHub Pages 프로젝트 페이지에서 깨진다
- **화면 파일(css·js)을 고치면 `index.html`의 `?v=` 숫자를 올려라.** 빌드가
  없어서 파일명에 해시가 안 붙는다 — 안 올리면 폰이 옛 CSS를 붙들고 새
  마크업과 섞는다(실제로 인트로 글자가 통째로 안 보이는 사고가 났다).
  규칙은 `index.html` 상단 주석에 있다
- **연출을 콘텐츠의 전제로 만들지 마라.** 기본 상태는 **보이는 것**이고
  등장 연출은 `@keyframes`의 `from`에만 둔다(fill-mode `backwards`).
  `animation`에 쓰는 `var()`에는 리터럴 폴백을 붙인다 — var 하나가 안 풀리면
  **선언 전체가 무효**가 되어 `opacity: 0`이 영구히 남는다. 위 사고의 뿌리다.
  **표시를 `animationend`·`transitionend`에 걸지 마라** — 질문의 선택
  피드백(150~250ms)도 시계로 잰다
- **`[hidden]{display:none!important}`를 이길 선언을 만들지 마라.** 화면
  전환이 전부 `el.hidden`인데 작성자 `display`가 그것을 이겨 죽은 적이 있다.
  `display`를 `!important`로 쓰는 곳은 그 규칙 하나뿐이고 검사가 지킨다
- **판단은 뷰모델, 그리기는 DOM 코드.** `view.js`(진입·설문·저장) ·
  `entry.js`(랜딩·기본확인·MASTER·범위·전환·게이트) · `result.js`(결과 여섯
  화면과 상세 셋) · `rows.js`(행 계약) · `format.js`(표시 형식)가 전부 DOM을
  모르는 순수함수이고 `test/view.test.mjs`가 본다.
  `app.js`(라우팅) · `screens.js`(진입 흐름 그리기) · `recovery.js`(결과
  그리기) · `render.js`(el·clear)는 그리기만 한다
- **시각은 `tokens.css` 하나다.** 색·글꼴·간격을 마크업·JS·다른 CSS에
  하드코딩하지 마라 — 시안이 바뀌면 그 파일만 갈아끼운다. 확정 팔레트는
  D-022고, **새벽 글로우(`--c-glow-*`)는 랜딩 전용**이다. 여명(`--c-accent`)은
  **행동 가능한 것**에만. 라이트 모드는 미대응이고 빈 블록으로 자리만 남겼다.
  **위험색은 없다** — 확정 와이어프레임의 빨강은 굵기·테두리·글자로 옮겼고
  최종 색은 시각 디자인 패스가 정한다
- **화면 갈래를 문자열로 추론하지 마라.** `하지 마세요`는
  `guidance_type=do_not`, `늦었어도 확인하세요`는 `missed` 버킷,
  `알아둘 내용`은 `awareness` + `waiting`이다. 제목이나 id를 읽지 않는다
- **엔진 키를 바꾸지 말고 화면에서 부르는 이름만 바꿔라.** `this_week` →
  `가까운 시일에`(NODE_LABEL), `몸`·`서류` → `건강`·`필요서류`(TOPIC_LABEL),
  `미판정` → `아직 확인 못 함`(STATUS_LABEL). **UI가 날짜·기한을 만들어
  내지 않는다** — `this_week`는 표시 버킷이라 `7일 안에`로 쓰면 거짓이 된다
- **`조건부`·`제외`·`미판정`을 뭉치지 마라.** 셋 다 엔진 `excluded` 버킷에
  있지만 주제 상세에서 각각 다른 자리다(D-023 §작은 결정 3)
- **UI에서도 `localStorage`를 직접 부르지 마라.** 인트로 플래그처럼 새 상태가
  필요하면 state 필드로 넣고 `saveState` 경유로 저장한다
- **연락처는 보류 모듈이다**(`contacts.js` · `screens.js`의 `renderContacts`).
  라우팅에 없고 지우지도 않는다. 임의로 번호를 넣거나 되살리지 마라
